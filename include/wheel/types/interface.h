#include <wheel/macros.h>
#include <wheel/misc/warnings.h>

#define compare LIBWHEEL_PREFIX(compare)
#define hash    LIBWHEEL_PREFIX(hash)
#define free    LIBWHEEL_PREFIX(free)
#define clone   LIBWHEEL_PREFIX(clone)
